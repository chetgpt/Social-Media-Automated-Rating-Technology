# Local artifacts

This ignored directory contains generated queues, historical exports, local
databases, diagnostics, reports, and samples moved out of the source root.
These files are not canonical MUSIC AUDIT output. Guarded audit artifacts stay
inside their immutable `comments_data/project_music_audit_*` directories.

The cleanup manifest under `docs/cleanup/` records the relocation categories.
