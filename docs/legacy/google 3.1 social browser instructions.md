# RETIRED — DO NOT EXECUTE

This file is a historical, non-authoritative training artifact. Its recorded
browser endpoints, login observations, commands, and screenshots are not
current evidence and are not an active browser or MUSIC AUDIT runbook.

For MUSIC AUDIT, use the authority order in `AGENTS.md` and `WORKFLOWS.md`, then
load `.agents/skills/google-3.1-music-audit-instructions/SKILL.md`. That skill's
Profile 7 recovery reference is the only model-specific browser recovery
procedure and is loaded only after the canonical collector reports a browser
blocker.

Never use this retired file to stop or broadly kill Edge, delete browser
control state, launch headless/direct-port/replacement-profile Edge, create a
scheduled-task or batch workaround, expose CDP endpoints, or bypass the exact
Profile 7 and workflow gates.
