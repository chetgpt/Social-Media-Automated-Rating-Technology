# Reusable configuration

- `campaign_specs/` contains reusable legacy campaign specifications and their
  runbook.
- `topic_profiles/` contains reusable topic-profile JSON.

Active canonical TikTok workflows remain governed by `AGENTS.md`; moving these
configuration files does not authorize a legacy collector.
