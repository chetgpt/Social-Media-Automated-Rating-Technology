# Maintainer tools

This directory is for bounded, reusable maintenance and export helpers. It is
not a workflow-bypass surface. Historical one-off scripts, direct database
mutators, manual approval helpers, and browser experiments belong in
`legacy/`, not here.
